# fgtc-specs
Bash script to quickly gather system specs and upload them to a build server

## Running
To run the script with the most up-to-date version, open a terminal and run:
```bash
curl https://raw.githubusercontent.com/FGTwinCities/fgtc-specs/main/fgtc-specs.sh | bash
```